import React from "react";
const Card = ({name, age}) => {
     return (
                <div>
                    <h3>Nombre: {name}</h3>
                    <h3>Edad: {age}</h3>
                    </div>
            );
        }
 export default Card;